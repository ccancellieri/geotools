<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="http://ehcache.org/ehcache.xsd"
	updateCheck="true" monitoring="autodetect" dynamicConfig="true">
	
	<diskStore path="java.io.tmpdir" />

	<!-- Store the status of the CacheManager (1 per cached data store instance) -->
	<cache name="CacheManagerStatus" maxElementsInMemory="100"
		eternal="false" timeToIdleSeconds="120" timeToLiveSeconds="120"
		overflowToDisk="true" maxElementsOnDisk="10000000" diskPersistent="true"
		diskExpiryThreadIntervalSeconds="120" memoryStoreEvictionPolicy="LRU">
		<!-- persistence strategy="localRestartable"
			synchronousWrites="false" /-->
		<!-- <persistence strategy="localRestartable|localTempSwap|none|distributed" 
			synchronousWrites="false|true"/> -->
	</cache>

	<!-- DEFAULT name="defaut" -->
	<defaultCache maxElementsInMemory="5"
		eternal="false" timeToIdleSeconds="20" timeToLiveSeconds="20"
		overflowToDisk="false" diskPersistent="true"
		memoryStoreEvictionPolicy="LRU">
		<!-- persistence strategy="localRestartable"
			synchronousWrites="false" /-->
		<!-- <persistence strategy="localRestartable|localTempSwap|none|distributed" 
			synchronousWrites="false|true"/> -->
	</defaultCache>

</ehcache>